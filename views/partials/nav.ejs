<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    .nav {
      width:100%;
      height: 13vh;
      background-color: transparent;
      display: flex;
      flex-direction: row;
      align-items: center;
      gap: 3rem;
      padding-right: 10px;
      border-bottom: 1px solid rgba(255,255,255,0.4);
      transition: background-color 0.3s, height 0.3s, border-bottom 0.3s;
    }

    .nav a {
      text-decoration: none;
      color: #afafb0;
      font-weight: 600;
      font-size: 18px;
    }

    .nav a:hover {
      text-shadow: 0 0 5px #7643ea;
      color: #fff;
    }

    .sticky {
      background-color: rgba(228, 227, 230, 0.9);
      height: 80px;
      z-index: 9999;
      border-bottom: 1px solid transparent;
    }

    .nav img {
      width: 70px;
      height:65px;
      object-fit:fill;
      margin-left: 10px;
      background: transparent;
    }

    .in-out {
      margin-left: auto;
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      gap: 15px;
      padding: 5px;
    }

    .in-out svg {
      width: 3rem;
    }
    .user-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: #000;
    color: #fff;
    font-size: 20px;
    font-weight: bold;
}
  </style>
</head>

<body>
  
  <div class="nav" id="navbar">
    <img src="\images\logo_2.png" alt="Logo"><p style="font-weight: bolder; color: #331a79; font-size: 20px;" >EXPLOREX</p>

    <a href="/home">Home</a>
    
    <a href="/itinerary">Customise</a>
    <a href="/contact">Add To Cart</a>
    <a href="/about">About Us</a>

    <div class="in-out">
      <% if (req.session.loggedIn) { %>
        <a href="/auth/logout" id="Logout">Logout</a>
        <div id="user-icon" class="user-icon"></div>
        <script>
            document.addEventListener("DOMContentLoaded", () => {
                let firstNameLetter = "<%= req.session.name.charAt(0).toUpperCase() %>";
                let userIconDiv = document.getElementById("user-icon");
                userIconDiv.innerHTML = `<span class="user-icon-text">${firstNameLetter}</span>`;
            });
        </script>
    <% } else { %>
        
        <a href="/auth" id="Login">Login</a>
      <% } %>
    </div>
  </div>

  <script>
    function account() {
      window.location.href = "/me";
    }

    window.onscroll = function() {
      const navbar = document.getElementById('navbar');
      if (window.pageYOffset > 0) {
        navbar.classList.add('sticky');
      } else {
        navbar.classList.remove('sticky');
      }
    };
  </script>
</body>

</html>
